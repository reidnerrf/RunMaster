describe('sample e2e', () => {
	it.skip('opens app and shows welcome', async () => {
		// Example: await device.launchApp();
		// Example: await expect(element(by.text('Pulse'))).toBeVisible();
	});
});